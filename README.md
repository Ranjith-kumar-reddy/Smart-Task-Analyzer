# Smart-Task-Analyzer
Smart task Analyzer is an intelligent task-prioritization system designed to help users identify project uses a custom scoring algorithm to evaluate tasks by urgency, importance,  The project works with backend of the application is built with Django, and API that receives task data in JSON format and returns ranked results.
